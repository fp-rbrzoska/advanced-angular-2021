import { MyStructDirective } from './my-struct.directive';

describe('MyStructDirective', () => {
  it('should create an instance', () => {
    const directive = new MyStructDirective();
    expect(directive).toBeTruthy();
  });
});
